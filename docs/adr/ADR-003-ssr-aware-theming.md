# ADR-003: SSR-aware theming

Status: accepted. Semantic CSS variables define themes. System mode is CSS-native; manual modes persist in a same-site cookie readable by SSR. Server and browser apply the same root attribute. Forced colors remains browser-controlled.
